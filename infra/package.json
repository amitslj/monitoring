{
  "name": "monitoring-infra",
  "version": "1.0.0",
  "description": "Standalone monitoring infrastructure using AWS CDK",
  "main": "launcher.ts",
  "scripts": {
    "build": "cd ../resource-inventory-lambda && npm install && npm run build && cd ../cloudwatch-alarm-lambda && npm install && npm run build && cd ../infra && tsc",
    "watch": "tsc -w",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "20.6.3",
    "@typescript-eslint/eslint-plugin": "^6.7.0",
    "@typescript-eslint/parser": "^6.7.0",
    "aws-cdk": "2.100.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "~5.2.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.100.0",
    "constructs": "^10.0.0"
  }
}
