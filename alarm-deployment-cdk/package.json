{
  "name": "alarm-deployment-cdk",
  "version": "1.0.0",
  "description": "CDK for deploying CloudWatch alarms based on inventory",
  "main": "app.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "deploy": "npm run build && npx cdk deploy --all --require-approval never",
    "destroy": "npx cdk destroy --all",
    "synth": "npm run build && npx cdk synth",
    "diff": "npm run build && npx cdk diff",
    "clean": "rm -rf dist cdk.out"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "20.6.3",
    "@typescript-eslint/eslint-plugin": "^6.7.0",
    "@typescript-eslint/parser": "^6.7.0",
    "aws-cdk": "2.100.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "~5.2.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.100.0",
    "constructs": "^10.0.0"
  }
}
