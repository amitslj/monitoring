{
  "name": "monitoring-module",
  "version": "1.0.0",
  "description": "AWS CloudWatch alarm deployment system using CDK",
  "private": true,
  "scripts": {
    "deploy": "cd alarm-deployment-cdk && npm install && npm run deploy",
    "destroy": "cd alarm-deployment-cdk && npm run destroy",
    "synth": "cd alarm-deployment-cdk && npm install && npm run synth",
    "diff": "cd alarm-deployment-cdk && npm install && npm run diff",
    "clean": "rm -rf alarm-deployment-cdk/dist alarm-deployment-cdk/cdk.out"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
